"""
Utility: Image OCR.

Purpose:
Extract text from standalone image files only after OCR is explicitly in scope.

Expected library:
pytesseract and Pillow

Pipeline role:
Used by MediaAdapter or OCR-specific adapter during Stage 1 Extract for image sources after the golden path proves core transformation quality.

Status:
Placeholder only. Not wired into the orchestrator yet.
"""
