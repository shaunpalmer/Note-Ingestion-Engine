"""
Utility: InDesign metadata extraction.

Purpose:
Extract basic metadata from InDesign files and honestly report unsupported or metadata-only status where text extraction is not available.

Expected library:
Optional ExifTool system dependency; dedicated parser not selected yet.

Pipeline role:
Used by AdobeAdapter during Stage 1 Extract for .indd sources after core document flows are proven.

Status:
Placeholder only. Not wired into the orchestrator yet.
"""
