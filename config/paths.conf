# Local Automation Stack — Path Configuration
# Sourced by scripts to keep paths DRY and configurable
# All paths are absolute. Edit this file if the project moves.

# ---------------------------------------------------------------------------
# Project Root
# ---------------------------------------------------------------------------
PROJECT_ROOT="/run/media/mpc/Expansion/_System_Snapshot_2026/Documents/LinuxBox-Vault/automation-scripts"

# ---------------------------------------------------------------------------
# Core Folders
# ---------------------------------------------------------------------------
CONFIG_DIR="${PROJECT_ROOT}/config"
LOGS_DIR="${PROJECT_ROOT}/logs"
REPORTS_DIR="${PROJECT_ROOT}/reports"
DATA_DIR="${PROJECT_ROOT}/data"
INBOX_DIR="${PROJECT_ROOT}/inbox"
ARCHIVE_DIR="${PROJECT_ROOT}/archive"
REVIEW_DIR="${PROJECT_ROOT}/review"
SCRIPTS_DIR="${PROJECT_ROOT}/scripts"
STATE_DIR="${PROJECT_ROOT}/state"

# ---------------------------------------------------------------------------
# Default Log File
# ---------------------------------------------------------------------------
AUTOMATION_LOG="${LOGS_DIR}/automation.log"

# ---------------------------------------------------------------------------
# Data Files
# ---------------------------------------------------------------------------
SCRIPT_REGISTRY="${DATA_DIR}/script-registry.csv"
STORAGE_TABLE="${DATA_DIR}/storage-table.csv"
RESOURCE_INVENTORY="${DATA_DIR}/resource-inventory.csv"

# ---------------------------------------------------------------------------
# Obsidian Paths
# ---------------------------------------------------------------------------
OBSIDIAN_VAULT="/home/mpc/Documents/LinuxBox-Vault"
OBSIDIAN_INBOX="${OBSIDIAN_VAULT}/Inbox"

# ---------------------------------------------------------------------------
# Document Ingestion Pipeline
# ---------------------------------------------------------------------------
# Where incoming text/markdown files land before processing
INGESTION_INCOMING="/home/mpc/Documents/LinuxBox-Vault/automation-scripts/imported-text-files"

# Where processed sidecar files (.ai.md) are written
INGESTION_SIDECARS="${PROJECT_ROOT}/sidecars"

# Where originals are moved after successful processing (optional)
INGESTION_PROCESSED="${PROJECT_ROOT}/processed"

# Where failed files are quarantined for review
INGESTION_FAILED="${PROJECT_ROOT}/failed"

# State tracking for resume capability
INGESTION_STATE="${STATE_DIR}/processed-files.json"

# Daily report output
INGESTION_REPORTS="${REPORTS_DIR}"

# ---------------------------------------------------------------------------
# Ollama Configuration
# ---------------------------------------------------------------------------
OLLAMA_HOST="http://127.0.0.1:11434"
OLLAMA_MODEL="qwen2.5-coder:7b"
OLLAMA_FALLBACK_MODEL="mistral"
OLLAMA_KEEP_ALIVE="3h"

# ---------------------------------------------------------------------------
# Network / Storage
# ---------------------------------------------------------------------------
STORAGE_SERVER="192.168.3.85"
STORAGE_PATH="/srv/shared"
